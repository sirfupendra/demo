spring.application.name=demo

# ? MongoDB Atlas connection
spring.data.mongodb.uri=mongodb+srv://upendrasisodiyacs22_db_user:XftuIl3nxeioD24r@springdb.07sqihz.mongodb.net/springdb?retryWrites=true&w=majority&appName=springdb

# (optional) Database name (if not included in URI)
spring.data.mongodb.database=springdb

# Optional: Server port
server.port=8080

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=2KB

# Logging configuration
logging.level.com.example.demo=INFO
logging.level.org.springframework.web=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Jackson configuration for JSON processing
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.write-dates-as-timestamps=false
